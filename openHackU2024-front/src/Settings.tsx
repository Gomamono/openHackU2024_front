import React from 'react';

const Settings: React.FC = () => {
  return (
    <div>
      <h1>Settings</h1>
      <p>ここでユーザーの設定を変更することができます。</p>
      {/* 設定項目を追加してください */}
    </div>
  );
};

export default Settings;
